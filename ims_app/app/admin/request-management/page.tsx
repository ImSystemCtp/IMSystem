"use client";
import {  RequestManagement } from "@/root/components";
export default function RequestManagementPage() {
    return (
        <main className="">
            <RequestManagement/>
        </main>
    );
}