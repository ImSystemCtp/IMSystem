"use client";
import { RegisterAssets } from "@/root/components";
export default function RegisterAssetsPage() {
    return (
        <main className="">
        <RegisterAssets/>
        </main>
    );
}